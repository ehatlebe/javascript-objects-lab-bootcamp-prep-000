let recipes = { enchiladas: 2 };

let updateObjectWithKeyAndValue = (object, key, value) => {
  return Object.assign(recipes, { [poppyseed cake]: 1 })
  // return Object.assign(playlist, { [artistName]: songTitle })
};
